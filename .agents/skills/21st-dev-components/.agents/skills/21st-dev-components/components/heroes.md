# 21st.dev Component Skill: Heroes

> **Priority:** CRITICAL
> **Category:** 21st.dev Design System Archive
> **Total Variants on 21st.dev:** 73+
> **Training Data:** 200 production-grade component prompts analyzed
> **Purpose:** Generate award-winning, production-ready Heroes components with unique designs every time.

---

## 1. Core Techniques

The following techniques were found across the 200 analyzed Heroes implementations:

- **ARIA accessibility attributes**
- **AnimatePresence exit animations**
- **CSS gradients**
- **CSS keyframe animations**
- **CVA (class-variance-authority) variants**
- **Drag and drop**
- **Framer Motion animations**
- **Framer Motion spring physics**
- **Framer Motion useMotionValue**
- **Framer Motion useTransform**
- **GLSL shaders**
- **GSAP animation library**
- **Glassmorphism / backdrop blur**
- **HTML Canvas rendering**
- **Intersection Observer / useInView**
- **Intersection Observer API**
- **Lottie animations**
- **Lucide React icons**
- **Next.js Image optimization**
- **Next.js Link routing**
- **Radix UI primitives**
- **React effects**
- **React refs**
- **React state management**
- **React useCallback optimization**
- **React useMemo optimization**
- **React.forwardRef pattern**
- **Recharts / charting**
- **ResizeObserver API**
- **Tailwind CSS Animate plugin**
- **Three.js / React Three Fiber**
- **cmdk command palette**
- **cn() utility (clsx + tailwind-merge)**

## 2. Dependencies & Libraries

Install commonly needed packages:
```bash
npm install @aliimam/icons @aliimam/logos @gsap/react @lottiefiles/dotlottie-react @paper-design/shaders-react @radix-ui/react-avatar @radix-ui/react-dialog @radix-ui/react-hover-card @radix-ui/react-label @radix-ui/react-progress @radix-ui/react-select @radix-ui/react-separator @radix-ui/react-slider @radix-ui/react-slot @radix-ui/react-switch @radix-ui/react-tabs @radix-ui/react-tooltip @react-three/csg @react-three/drei @react-three/fiber @react-three/postprocessing @splinetool/react-spline @splinetool/runtime @tsparticles/engine @tsparticles/react @tsparticles/slim class-variance-authority clsx d3 dicons framer-motion gsap lenis lodash lucide-react matter-js motion next next-themes poly-decomp postprocessing power2 prop-types qss r3f-perf react-icons react-progressive-blur react-typed react-use-measure simplex-noise svg-path-commander tailwind-merge tailwindcss three uuid
```

| Dependency | Purpose |
|---|---|
| `@aliimam/icons` | Utility |
| `@aliimam/logos` | Utility |
| `@gsap/react` | Professional animation |
| `@lottiefiles/dotlottie-react` | Utility |
| `@paper-design/shaders-react` | Utility |
| `@radix-ui/react-avatar` | Headless accessible primitives |
| `@radix-ui/react-dialog` | Headless accessible primitives |
| `@radix-ui/react-hover-card` | Headless accessible primitives |
| `@radix-ui/react-label` | Headless accessible primitives |
| `@radix-ui/react-progress` | Headless accessible primitives |
| `@radix-ui/react-select` | Headless accessible primitives |
| `@radix-ui/react-separator` | Headless accessible primitives |
| `@radix-ui/react-slider` | Headless accessible primitives |
| `@radix-ui/react-slot` | Headless accessible primitives |
| `@radix-ui/react-switch` | Headless accessible primitives |
| `@radix-ui/react-tabs` | Headless accessible primitives |
| `@radix-ui/react-tooltip` | Headless accessible primitives |
| `@react-three/csg` | 3D rendering |
| `@react-three/drei` | 3D rendering |
| `@react-three/fiber` | 3D rendering |
| `@react-three/postprocessing` | 3D rendering |
| `@splinetool/react-spline` | Utility |
| `@splinetool/runtime` | Utility |
| `@tsparticles/engine` | Utility |
| `@tsparticles/react` | Utility |
| `@tsparticles/slim` | Utility |
| `class-variance-authority` | Variant management |
| `clsx` | Class composition |
| `d3` | Utility |
| `dicons` | Utility |
| `framer-motion` | Physics-based animations |
| `gsap` | Professional animation |
| `lenis` | Utility |
| `lodash` | Utility |
| `lucide-react` | SVG icon library |
| `matter-js` | Utility |
| `motion` | Physics-based animations |
| `next` | Next.js framework feature |
| `next-themes` | Next.js framework feature |
| `poly-decomp` | Utility |
| `postprocessing` | Utility |
| `power2` | Utility |
| `prop-types` | Utility |
| `qss` | Utility |
| `r3f-perf` | Utility |
| `react-icons` | Utility |
| `react-progressive-blur` | Utility |
| `react-typed` | Utility |
| `react-use-measure` | Utility |
| `simplex-noise` | Utility |
| `svg-path-commander` | Utility |
| `tailwind-merge` | Class composition |
| `tailwindcss` | Utility |
| `three` | 3D rendering |
| `uuid` | Utility |

## 3. Real Component Archetypes

These are actual component implementations found in the 21st.dev registry:

- `3d-gallery-photography.tsx`
- `3d-hero-section-boxes.tsx`
- `3d-testimonails.tsx`
- `GridBeam.tsx`
- `a-modern-hero-section.tsx`
- `about-3.tsx`
- `acme-hero.tsx`
- `aether-hero.tsx`
- `ai-image-generator-hero.tsx`
- `ai-input-hero.tsx`
- `animate-card-animation.tsx`
- `animated-gradient-background.tsx`
- `animated-gradient-text.tsx`
- `animated-hero-section.tsx`
- `animated-hero.tsx`
- `animated-roadmap.tsx`
- `animated-shader-background.tsx`
- `animated-shader-hero.tsx`
- `animated-video-on-scroll.tsx`
- `arc-gallery-hero-component.tsx`
- ...and 167 more variants

## 4. Props & Variant Patterns

### CVA Variant Names Found
- `above`
- `below`
- `bottom`
- `center`
- `default`
- `default (size)`
- `destructive`
- `ghost`
- `icon (size)`
- `large (size)`
- `lg (size)`
- `link`
- `outline`
- `secondary`
- `sm (size)`
- `small (size)`
- `top`

### Custom Props Found
- `Breathing`
- `action`
- `actions`
- `alt`
- `anchor`
- `animate`
- `animatePresenceInitial`
- `animatePresenceMode`
- `animated`
- `animation`
- `animationSequence`
- `animationSpeed`
- `animationStyle`
- `announcement`
- `ariaLabel`
- `ariaLive`
- `ariaRole`
- `as`
- `asChild`
- `auto`

## 5. Code Patterns (From Real Implementations)

### Pattern 1
```tsx
import { cn } from "@/lib/utils"

const Card = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn(
      "rounded-lg border bg-card text-card-foreground shadow-sm",
      className,
    )}
    {...props}
  />
))
Card.displayName = "Card"

const CardHeader = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn("flex flex-col space-y-1.5 p-6", className)}
    {...props}
  />
))
```

### Pattern 2
```tsx
 * @param children contains react node elements.
 */
export function ShineBorder({
  borderRadius = 8,
  borderWidth = 1,
  duration = 14,
  color = "#000000",
  className,
  children,
}: ShineBorderProps) {
  return (
    <div
      style={
        {
          "--border-radius": `${borderRadius}px`,
        } as React.CSSProperties
      }
      className={cn(
        "min-h-[60px] w-fit min-w-[300px] place-items-center rounded-[--border-radius] bg-white p-3 text-black dark:bg-black dark:text-white",
        className,
      )}
    >
      <div
        style={
          {
            "--border-width": `${borderWidth}px`,
            "--border-radius": `${borderRadius}px`,
```

### Pattern 3
```tsx
import { cn } from "@/lib/utils"

const buttonVariants = cva(
  "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground hover:bg-primary/90",
        destructive:
          "bg-destructive text-destructive-foreground hover:bg-destructive/90",
        outline:
          "border border-input bg-background hover:bg-accent hover:text-accent-foreground",
        secondary:
          "bg-secondary text-secondary-foreground hover:bg-secondary/80",
        ghost: "hover:bg-accent hover:text-accent-foreground",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-10 px-4 py-2",
        sm: "h-9 rounded-md px-3",
        lg: "h-11 rounded-md px-8",
        icon: "h-10 w-10",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
```

## 6. Integration Workflow

1. Create the component file in `/components/ui/hero.tsx`
2. Install required dependencies: `@aliimam/icons, @aliimam/logos, @gsap/react`
3. Ensure `cn()` utility exists at `@/lib/utils`
4. Use `cva()` for variant management when the component has multiple visual states
5. Wrap with `React.forwardRef` for composability with form libraries
6. Add `displayName` for DevTools debugging
7. Export both the component and any variant helper (e.g., `heroVariants`)

## 7. Anti-Patterns

- **[ANTI-PATTERN]** Using inline styles instead of Tailwind utility classes
- **[ANTI-PATTERN]** Hardcoding colors — always use CSS custom properties or Tailwind theme tokens
- **[ANTI-PATTERN]** Missing keyboard navigation — all interactive Heroes must be keyboard-accessible
- **[ANTI-PATTERN]** No `focus-visible` ring — always provide visible focus indicators
- **[ANTI-PATTERN]** Omitting `aria-*` attributes on interactive elements
- **[ANTI-PATTERN]** Using `px` units for responsive dimensions — prefer `rem`, `%`, or Tailwind spacing
- **[ANTI-PATTERN]** Instant state changes without transitions — use `framer-motion` springs

## 8. Design Philosophy

When generating Heroes components:

1. **Never repeat a design** — with 73+ archetypes available, each generation should be unique
2. **Layer composition** — combine background effects, surface treatments, and interaction layers
3. **Micro-interactions** — hover states, focus rings, active scales, entrance animations
4. **Dark mode first** — design for dark backgrounds, add light mode as enhancement
5. **Production-ready** — TypeScript strict, accessible, responsive, performant

---

## 9. 🧬 Generative DNA — Randomization Matrix

> **How to use:** When generating a Heroes component, pick ONE option from EACH axis below to create a unique combination. Never use the same combination twice.

### 📐 Layout Axis
1. Full-viewport centered headline
2. Split 50/50 (text left, visual right)
3. Video/shader background with overlay text
4. Stacked sections with scroll-triggered stages
5. Asymmetric diagonal composition

### 🎨 Surface Axis
1. Animated GLSL shader background
2. Video loop with dark overlay
3. Gradient mesh with floating blobs
4. Image hero with parallax layers
5. Particle field (tsparticles/canvas)

### ⚡ Motion Axis
1. Headline typewriter or word-flip reveal
2. Parallax layers on mouse move
3. Scroll-triggered reveal stages
4. CTA button magnetic + glow
5. Background continuously animates (shader/particles)

### 🎭 Mood Axis
1. SaaS launch — bold headline, sub-text, dual CTAs
2. Portfolio — name, title, scroll indicator, minimal
3. Agency — dramatic, full-bleed video, overlay text
4. Product — 3D mockup, feature badges, conversion
5. Artistic — experimental layout, shader bg, abstract

### 🧩 Composition Axis
1. Landing page first fold
2. Product homepage
3. Portfolio entry point
4. Documentation welcome
5. App marketing page

## 10. 🎲 Spawn Recipes — Ready-to-Build Blueprints

> Each recipe is a complete design brief. Pick one as a starting point, then apply Chaos Modifiers (Section 11) to make it unique.

### Recipe 1: Shader Void
full-viewport with custom GLSL shader background (dark flowing waves). Massive headline (text-7xl) fades up with stagger per word. Sub-text in muted color. Two CTA buttons: primary (gradient + glow) and secondary (outline). Scroll indicator chevron bounces at bottom.

### Recipe 2: Split Product
left 50%: headline, description, CTA buttons, trust badge row. Right 50%: 3D product mockup with subtle rotation on mouse move. Headline uses gradient text (brand spectrum). Trust badges stagger in. Right visual has glow shadow.

### Recipe 3: Parallax Layers
full-viewport with 3 parallax layers: background (gradient/image), midground (geometric shapes), foreground (text content). Mouse movement creates depth effect. Headline is text-7xl bold. Mouse-tracking glow spotlight effect on headline.

### Recipe 4: Video Cinematic
looping video background (muted, poster frame). Dark overlay (black/50). Centered content: announcement badge, headline, description, CTA button. Content reveals with stagger. Video has scanline overlay at 3%. Button has magnetic hover.

## 11. 🌀 Chaos Modifiers

> Stack 1-3 Chaos Modifiers from the **master SKILL.md** to add unique character. See the full list of 15 modifiers with CSS/JS implementation details there.

---

## 12. 📍 When to Use — Page Context

This component appears in these page types:

- **Landing page first fold**
- **Product homepage**
- **Portfolio entry**
- **App marketing**

## 13. 🔗 Composes With

Load these companion blueprints when building with Heroes:

- `components/buttons.md` — Buttons
- `components/backgrounds.md` — Backgrounds
- `components/badges.md` — Badges
- `components/clients.md` — Clients
- `components/images.md` — Images

